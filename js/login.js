import { supabase } from "./config.js"; const form = document.querySelector("#login-form"); const message = document.querySelector("#login-message"); async function redirectExistingSession() { const { data, error } = await supabase.auth.getSession(); if (error) { console.error("Unable to check session:", error); return; } if (data.session) { window.location.href = "./dashboard.html"; } } form.addEventListener("submit", async (event) => { event.preventDefault(); message.textContent = "Signing in..."; const email = document.querySelector("#email").value.trim(); const password = document.querySelector("#password").value; const { error } = await supabase.auth.signInWithPassword({ email, password }); if (error) { console.error("Sign-in error:", error); message.textContent = "Sign-in failed. Check your email and password."; return; } window.location.href = "./dashboard.html"; }); redirectExistingSession();