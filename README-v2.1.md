# Version 2.1 Role Update

Run `database/v2_1-role-permissions.sql` in Supabase after backing up.

Roles: administrator, office_manager, senior_preparer, preparer, receptionist, bookkeeper, read_only.

Keep your existing `js/config.js`. Copy the replacement files into VS Code, restart Live Server, and test each role with fictional users.
